<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/editJournal.css" />
    <title>Edit Journal Entry</title>
  </head>
  <body>
    <header>
      <h1>Edit Journal Entry</h1>
    </header>
    <section class="section">
      <div class="container">
        <form action="/edit/<%= journal._id %>?_method=PUT" method="POST">
          <div class="form-group">
            <label for="title" class="journal-label">Title:</label>
            <input
              type="text"
              id="title"
              name="title"
              value="<%= journal.title %>"
              class="entry-text-title"
              required
            />
          </div>
          <div class="form-group">
            <label for="description" class="journal-label">Description:</label>
            <textarea
              id="description"
              name="description"
              rows="5"
              class="entry-text-box"
              required
            ><%= journal.description %></textarea>
          </div>
          <div class="form-actions">
            <button type="submit" class="btn-main">Update Entry</button>
            <a href="/journalEntries" class="btn-main cancel-btn">Cancel</a>
          </div>
        </form>
      </div>
    </section>
  </body>
</html>


